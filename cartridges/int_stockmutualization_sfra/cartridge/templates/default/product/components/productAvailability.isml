<!-- Availability -->
<div class="row availability product-availability" data-ready-to-order="${product.readyToOrder}" data-available="${product.SMAvailable ? product.SMAvailable : product.available}">
    <isinclude template="product/components/availability" />
</div>