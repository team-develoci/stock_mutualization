<iscontent type="text/html" charset="UTF-8" compact="true"/>
<iscomment>
Prints a message about the product's availability.
</iscomment>

<div class="availability-msg">
    <p class="${pdict.availabilityClass}">${pdict.availabilityMsg}</p>
    <isif condition="${pdict.inStockDateMsg}"><p class="in-stock-date-msg">${pdict.inStockDateMsg}</p></isif>
</div>
